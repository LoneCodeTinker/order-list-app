{
  "name": "order-list-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "npm run prestart && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "backend": "cd backend && uvicorn main:app --reload",
    "dev:all": "concurrently \"npm run backend\" \"npm run dev\"",
    "prestart": "pip install -r backend/requirements.txt",
    "build:frontend": "vite build",
    "build:backend": "pip install -r backend/requirements.txt && echo Backend ready. && npm install react-qr-barcode-scanner",
    "start:backend": "cd backend && uvicorn main:app --host 0.0.0.0 --port 8000",
    "start:all": "npm run build:backend && npm run build && concurrently \"npm run start:backend\" \"npx serve dist --single --listen 5173\""
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-qr-barcode-scanner": "^2.1.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "@types/react": "^19.1.5",
    "@types/react-dom": "^19.1.5",
    "@vitejs/plugin-react": "^4.4.1",
    "concurrently": "^7.6.0",
    "eslint": "^9.25.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "globals": "^16.0.0",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.30.1",
    "vite": "^6.3.5"
  }
}
